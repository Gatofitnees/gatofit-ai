
import { supabase } from "@/integrations/supabase/client";

// Función vacía para inicializar rutinas predefinidas
// Se ha eliminado toda la funcionalidad de rutinas predefinidas según lo solicitado
export const initPredefinedRoutines = async () => {
  console.log("Rutinas predefinidas desactivadas por petición del usuario");
  return true;
};
